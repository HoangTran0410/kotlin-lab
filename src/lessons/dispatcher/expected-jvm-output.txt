1. coroutine body runs on the IO pool — inherited from the scope's context
2. withContext switches to the Default pool: still the SAME coroutine, different thread
3. leaving withContext, back on the IO pool
4. scope is cancelled — so is every child of it
