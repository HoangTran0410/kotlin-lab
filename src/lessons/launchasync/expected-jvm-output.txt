1. launch finished — it doesn't return any value
2. join() only WAITS, it can't read anything
3. await() both waits and returns a value: 42
4. the Deferred already failed a while ago, but nobody has read it yet so nobody has seen it
5. await() throws the exception at the EXACT spot await is called: boom
6. supervisorScope blocks the async's failure, the program continues
