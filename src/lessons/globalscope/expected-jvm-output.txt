1. both coroutines run at the same time, but only ONE hangs under runBlocking
2. child IN the tree — runBlocking waits for it before finishing
3. main is done — the GlobalScope coroutine is left stranded mid-flight
